{
  "name": "@nycopportunity/pattern-elements",
  "version": "0.0.0-16",
  "description": "Shared elements for the NYC Opportunity UI Patterns Framework.",
  "scripts": {
    "version": "git add .",
    "prepublishOnly": "git push && git push --tags"
  },
  "dependencies": {
    "deepmerge": "^4.2.2"
  }
}
